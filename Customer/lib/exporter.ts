import { customerRegister, getCustomerList, getCustomerProfile } from './customer/customer-controller';

const customer = {
    customerRegister,
    getCustomerList,
    getCustomerProfile,
};

export default customer;
